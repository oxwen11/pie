# Docs and tooling

Allowed when the full diff cannot change functional product behavior:

- docs, CI, tooling, and agent rules;
- root `package.json` or the root lockfile only for oxlint, oxfmt, or their plugins;
- pkg.pr.new previews.
