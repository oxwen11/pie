diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index e8e8859..d0fce35 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -10,7 +10,7 @@ import * as _bundledPiAgentCore from "@earendil-works/pi-agent-core";
 import * as _bundledPiAiCompat from "@earendil-works/pi-ai/compat";
 import * as _bundledPiAiOauth from "@earendil-works/pi-ai/oauth";
 import * as _bundledPiAiProviders from "@earendil-works/pi-ai/providers/all";
-import * as _bundledPiTui from "@earendil-works/pi-tui";
+const _bundledPiTui = {};
 import { createJiti } from "jiti/static";
 // Static imports of packages that extensions may use.
 // These MUST be static so Bun bundles them into the compiled binary.
@@ -21,7 +21,7 @@ import * as _bundledTypeboxValue from "typebox/value";
 import { CONFIG_DIR_NAME, getAgentDir, isBunBinary, isBundledNode } from "../../config.js";
 // NOTE: This import works because loader.ts exports are NOT re-exported from index.ts,
 // avoiding a circular dependency. Extensions can import from @earendil-works/pi-coding-agent.
-import * as _bundledPiCodingAgent from "../../index.js";
+import * as _bundledPiCodingAgent from "../sdk.js";
 import { resolvePath } from "../../utils/paths.js";
 import { createEventBus } from "../event-bus.js";
 import { execCommand } from "../exec.js";
diff --git a/dist/modes/interactive/theme/theme.js b/dist/modes/interactive/theme/theme.js
index 5418167..d9de2a1 100644
--- a/dist/modes/interactive/theme/theme.js
+++ b/dist/modes/interactive/theme/theme.js
@@ -6,6 +6,8 @@ import { getCustomThemesDir, getThemesDir } from "../../../config.js";
 import { closeWatcher, watchWithErrorHandler } from "../../../utils/fs-watch.js";
 import { highlight, supportsLanguage } from "../../../utils/syntax-highlight.js";
 import { stripBom } from "../../../utils/text.js";
+import darkThemeJson from "./dark.json" with { type: "json" };
+import lightThemeJson from "./light.json" with { type: "json" };
 let themeJsonValidator;
 /**
  * Install full theme validation. Without it, documents are accepted as-is, which is what built-in
@@ -270,13 +272,7 @@ export class Theme {
 let BUILTIN_THEMES;
 function getBuiltinThemes() {
     if (!BUILTIN_THEMES) {
-        const themesDir = getThemesDir();
-        const darkPath = path.join(themesDir, "dark.json");
-        const lightPath = path.join(themesDir, "light.json");
-        BUILTIN_THEMES = {
-            dark: JSON.parse(stripBom(fs.readFileSync(darkPath, "utf-8"))),
-            light: JSON.parse(stripBom(fs.readFileSync(lightPath, "utf-8"))),
-        };
+        BUILTIN_THEMES = { dark: darkThemeJson, light: lightThemeJson };
     }
     return BUILTIN_THEMES;
 }
