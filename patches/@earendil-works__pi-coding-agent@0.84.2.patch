diff --git a/dist/modes/rpc/rpc-mode.js b/dist/modes/rpc/rpc-mode.js
index a739ca7e20916ba737600e96f842e1cf4561afce..1719d188fafcfd6b2811b173c9dd1605460a1bca 100644
--- a/dist/modes/rpc/rpc-mode.js
+++ b/dist/modes/rpc/rpc-mode.js
@@ -330,6 +330,10 @@ export async function runRpcMode(runtimeHost) {
                 await session.abort();
                 return success(id, "abort");
             }
+            case "clear_queue": {
+                const data = session.clearQueue();
+                return success(id, "clear_queue", data);
+            }
             case "new_session": {
                 const options = command.parentSession ? { parentSession: command.parentSession } : undefined;
                 const result = await runtimeHost.newSession(options);
diff --git a/dist/modes/rpc/rpc-types.d.ts b/dist/modes/rpc/rpc-types.d.ts
index b772bc7b7522384202532c68b4615658789bd771..82b5793d34cb04c1772e0ca0e664ece6b0a5c7ce 100644
--- a/dist/modes/rpc/rpc-types.d.ts
+++ b/dist/modes/rpc/rpc-types.d.ts
@@ -30,6 +30,9 @@ export type RpcCommand = {
 } | {
     id?: string;
     type: "abort";
+} | {
+    id?: string;
+    type: "clear_queue";
 } | {
     id?: string;
     type: "new_session";
@@ -176,6 +179,15 @@ export type RpcResponse = {
     type: "response";
     command: "abort";
     success: true;
+} | {
+    id?: string;
+    type: "response";
+    command: "clear_queue";
+    success: true;
+    data: {
+        steering: string[];
+        followUp: string[];
+    };
 } | {
     id?: string;
     type: "response";
